@using MilitaryProject.Domain.ViewModels.Request
@model RequestViewModel

@{
    ViewData["Title"] = "Delete Request";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <h1 class="card-header text-center">Delete Request</h1>
                <div class="card-body">
                    <h4>Are you sure you want to delete this request?</h4>
                    <div>
                        <label>Brigade ID:</label>
                        <span>@Model.BrigadeID</span>
                    </div>
                    <form asp-controller="Request" asp-action="DeleteRequest" method="post">
                        <input type="hidden" asp-for="ID" />
                        <button type="submit" class="btn btn-danger mt-3">Delete</button>
                        <a asp-controller="Request" asp-action="Index" class="btn btn-secondary mt-3">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
