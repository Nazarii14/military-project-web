﻿@using MilitaryProject.Domain.ViewModels.Stats
@model List<StatsAmmunitionViewModel>

@{
    ViewData["Title"] = "Ammunition";
}

<style>
    .custom-container {
        margin-top: 50px;
    }

    .custom-row {
        justify-content: center;
    }

    .custom-col-md-8 {
        text-align: center;
    }

    .custom-btn {
        padding: 8px 16px;
        border-radius: 4px;
        text-decoration: none;
        display: inline-block;
        font-size: 24px;
    }

    .custom-btn-success {
        background-color: #28a745;
        color: #fff;
    }

    .custom-btn-outline-primary {
        background-color: transparent;
        border: 1px solid #007bff;
        color: #007bff;
    }

    .custom-btn-primary {
        background-color: #007bff;
        color: #fff;
    }

    .custom-btn-danger {
        background-color: #dc3545;
        color: #fff;
    }

    .custom-table {
        width: 100%;
        margin-top: 16px;
    }

        .custom-table th,
        .custom-table td {
            padding: 10px;
            text-align: center;
            font-size: 24px;
        }

        .custom-table thead {
            background-color: #007bff;
            color: #fff;
        }

        .custom-table tbody tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        .custom-table tbody tr:hover {
            background-color: #e2e2e2;
        }

    .custom-text-center {
        text-align: center;
        font-size: 3.5em;
    }

    .custom-btn custom-btn-primary {
        font-size: 24px;
    }

    .custom-btn-primary:hover {
        background-color: mediumspringgreen;
    }

    .custom-btn-danger:hover {
        background-color: red;
        color: white;
    }

</style>

<div class="custom-container mt-5">
    <div class="custom-row justify-content-center">
        <div class="custom-col-md-8">
            <h2 class="custom-text-center">Ammunition</h2>
            <a class="custom-btn custom-btn-success" asp-controller="StatsAmmunition" asp-action="Create">Create</a>
            <table class="custom-table">
                <thead>
                    <tr>
                        <th class="custom-name">Name</th>
                        <th class="custom-count">Count</th>
                        <th class="custom-needed-count">Needed count</th>
                        <th class="custom-details">Details</th>
                        <th class="custom-edit">Edit</th>
                        <th class="custom-action">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var ammunition in Model)
                    {
                        <tr>
                            <td class="custom-name">@ammunition.AmmunitionName</td>
                            <td class="custom-count">@ammunition.AmmunitionCount</td>
                            <td class="custom-needed-count">@ammunition.NeededAmmunitionCount</td>
                            <td class="custom-details"><a class="custom-btn custom-btn-outline-primary" asp-controller="StatsAmmunition" asp-action="GetById" asp-route-ammunitionID="@ammunition.AmmunitionID">Details</a></td>
                            <td class="custom-edit"><a class="custom-btn custom-btn-primary" asp-controller="StatsAmmunition" asp-action="Update" asp-route-ammunitionID="@ammunition.AmmunitionID">Edit</a></td>
                            <td class="custom-action"><a class="custom-btn custom-btn-danger" asp-controller="StatsAmmunition" asp-action="Delete" asp-route-ammunitionID="@ammunition.AmmunitionID">Delete</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>